import { AppDragDropDirective } from './app-drag-drop.directive';

describe('AppDragDropDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDragDropDirective();
    expect(directive).toBeTruthy();
  });
});
